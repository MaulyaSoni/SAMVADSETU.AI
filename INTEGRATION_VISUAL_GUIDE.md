# 🎨 VISUAL INTEGRATION GUIDE

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERACTION                         │
│                                                             │
│  1. Open http://localhost:3000                            │
│  2. Go to "Recognize" page                                │
│  3. Click "Start Camera"                                  │
│  4. Click "Capture Frame"                                 │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
        ┌──────────────────────────┐
        │  Next.js Web App         │
        │  (Port 3000)             │
        │  ┌────────────────────┐  │
        │  │ asl-recognizer.tsx │  │
        │  │  - Video capture   │  │
        │  │  - Frame to Base64 │  │
        │  │  - Send to API     │  │
        │  └────────────────────┘  │
        └──────────────┬───────────┘
                       │
          ┌────────────┴────────────┐
          │                         │
          ↓                         ↓
   ┌─────────────┐         ┌──────────────┐
   │  Backend    │         │  Fallback    │
   │  API Server │         │ TensorFlow.js│
   │ (Port 5000) │         │  (Browser)   │
   └─────────────┘         └──────────────┘
          │                         │
          ↓                         │
   ┌──────────────────────────┐    │
   │  Unified Model Loader    │    │
   │                          │    │
   │  Load all 3 models       │    │
   │  on startup              │    │
   └──────────┬───────────────┘    │
              │                    │
         ┌────┴────┬──────┐       │
         ↓         ↓      ↓       │
      ┌──┐      ┌──┐   ┌──┐      │
      │M1│      │M2│   │M3│      │
      └──┘      └──┘   └──┘      │
       ASL    MNIST   HaGRID     │
              │                   │
              ↓                   │
        ┌──────────────┐         │
        │ Prediction   │         │
        │ + Confidence │         │
        └──────┬───────┘         │
               │                 │
               │    ┌────────────┘
               │    │ (if API fails)
               │    │ Fall back to
               │    │ client-side
               │    ↓
               └─→ ┌─────────────────┐
                   │ Display Results │
                   │ - Prediction    │
                   │ - Confidence %  │
                   │ - Top 3         │
                   │ - Model used    │
                   └─────────────────┘
```

---

## Data Flow Diagram

```
Camera Frame
     │
     ↓
Canvas.drawImage()
     │
     ↓
canvas.toDataURL("image/jpeg")
     │
     ↓
Extract Base64
     │
     ↓
┌─────────────────────┐
│  API Request JSON   │
├─────────────────────┤
│ {                   │
│   "image": "...",   │
│   "model": "...",   │
│   "threshold": 0.5  │
│ }                   │
└──────────┬──────────┘
           │
           ↓
POST to http://localhost:5000/api/models/predict
           │
           ↓
┌──────────────────────────┐
│  Flask Server            │
│  1. Decode Base64       │
│  2. Load image          │
│  3. Resize to model     │
│  4. Normalize           │
│  5. Predict             │
└──────────┬───────────────┘
           │
           ↓
┌──────────────────────────┐
│  Response JSON           │
├──────────────────────────┤
│ {                        │
│   "prediction": "A",     │
│   "confidence": 0.95,    │
│   "model": "asl...",     │
│   "all_predictions": {   │
│     "A": 0.95,           │
│     "B": 0.02, ...       │
│   },                     │
│   "success": true        │
│ }                        │
└──────────┬───────────────┘
           │
           ↓
Display in React Component
```

---

## File Organization

```
Samvad_Setu_final/
│
├── 📁 scripts/
│   └── model_api_server.py          ← ⭐ Flask API Backend
│       (353 lines, auto-loads models)
│
├── 📁 notebooks/Saved_models/       ← ⭐ Model Files
│   ├── final_asl_model-training-optimized.keras
│   ├── final_sign_mnist_cnn.keras
│   └── HAGRID_best_model.keras
│
├── 📁 components/
│   └── asl-recognizer.tsx           ← ⭐ Updated for API
│       (Real-time prediction UI)
│
├── 📁 app/
│   ├── page.tsx                     (Main page)
│   ├── recognize/                   (Recognize page)
│   └── api/
│       ├── recognize/
│       ├── train/
│       └── collect/
│
├── 🚀 START_MODEL_API.bat           ← ⭐ Run this! (Windows)
├── 🚀 START_MODEL_API.sh            ← ⭐ Run this! (Linux/Mac)
├── ✓ verify_integration.py          ← ⭐ Check system health
│
├── 📖 INTEGRATION_INDEX.md          (Navigation guide)
├── 📖 INTEGRATION_QUICK_START.md    (5-min setup)
├── 📖 README_INTEGRATION.md         (Full technical docs)
├── 📖 MODELS_INTEGRATION_SUMMARY.md (Overview)
├── 📖 INTEGRATION_COMPLETE_SUMMARY.md (Final summary)
│
└── [Other existing files...]
```

---

## Setup Process Flow

```
START
  │
  ├─→ Run: START_MODEL_API.bat
  │   ├─→ Activate conda environment (asl310)
  │   ├─→ Check dependencies installed
  │   └─→ Launch Flask server (Port 5000)
  │       ├─→ Load ASL Alphabet Model
  │       ├─→ Load Sign MNIST Model
  │       ├─→ Load HaGRID Model
  │       └─→ Ready for API calls
  │
  ├─→ Run: npm run dev
  │   ├─→ Start Next.js dev server (Port 3000)
  │   ├─→ Compile TypeScript/React
  │   └─→ Ready for browser access
  │
  ├─→ Open: http://localhost:3000
  │   ├─→ View home page
  │   └─→ Navigate to /recognize
  │
  └─→ Use Application
      ├─→ Click "Start Camera"
      ├─→ Allow camera permissions
      ├─→ Click "Capture Frame"
      ├─→ Send to API
      ├─→ Get prediction
      └─→ Display result
          
SUCCESS ✅
```

---

## Model Selection Diagram

```
User uploads/captures image
         │
         ↓
    ┌────────────┐
    │ Which model│
    │   to use? │
    └────┬──────┘
         │
    ┌────┴─────────┬──────────┬──────────┐
    │              │          │          │
    ↓              ↓          ↓          ↓
┌────────┐     ┌─────────┐ ┌─────┐  ┌──────┐
│ASL     │     │ Sign    │ │Hag  │  │All 3 │
│Alpha   │     │ MNIST   │ │RID  │  │(Comp)│
└─┬──────┘     └────┬────┘ └──┬──┘  └───┬──┘
  │                 │        │        │
  │                 │        │        │
  ↓                 ↓        ↓        ↓
Full ASL  Letter Only  Hand Det.  Compare
A-Z + 3   A-Z Only    Yes/No    All together

Confidence Displayed:
- Individual scores
- Top 3 predictions
- Model source
- Success/failure
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────────┐
│              FRONTEND (Browser)                     │
├─────────────────────────────────────────────────────┤
│  • Next.js 14+          (React framework)           │
│  • TypeScript           (Type safety)               │
│  • TensorFlow.js        (Client-side fallback)      │
│  • MediaPipe           (Hand detection)            │
│  • OpenCV.js           (Image processing)          │
└─────────────────────────────────────────────────────┘
                          ↕
                    HTTP / JSON API
                          ↕
┌─────────────────────────────────────────────────────┐
│              BACKEND (Server)                       │
├─────────────────────────────────────────────────────┤
│  • Flask               (Python web framework)       │
│  • Flask-CORS          (Cross-origin support)       │
│  • TensorFlow/Keras    (Model inference)            │
│  • OpenCV             (Image processing)            │
│  • Pillow             (Image handling)              │
│  • NumPy              (Numerical computing)         │
└─────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────┐
│              MODELS (Keras Files)                   │
├─────────────────────────────────────────────────────┤
│  • ASL Alphabet         (29 classes)                │
│  • Sign MNIST          (26 classes)                 │
│  • HaGRID              (2 classes)                  │
└─────────────────────────────────────────────────────┘
```

---

## Decision Tree: Which Model?

```
What do you want to do?
│
├─→ Recognize full ASL alphabet?
│   └─→ Use: ASL_ALPHABET ✅
│        (Best accuracy, most classes)
│
├─→ Recognize only letters A-Z?
│   └─→ Use: SIGN_MNIST ✅
│        (Lighter, faster)
│
├─→ Detect if hand is present?
│   └─→ Use: HAGRID ✅
│        (Binary classification)
│
├─→ Compare all predictions?
│   └─→ Use: /api/models/compare ✅
│        (Get all at once)
│
└─→ Not sure?
    └─→ Default: ASL_ALPHABET ✅
         (Most comprehensive)
```

---

## Performance Characteristics

```
                    Speed        Accuracy    Offline   Fallback
                    ─────────────────────────────────────────────
Backend API         ⚡⚡⚡⚡⚡     99.5%      ❌        ✅
(GPU/CPU)           Very Fast                Yes

Client-side         ⚡⚡⚡        98%        ✅        ❌
(TensorFlow.js)     Fast                     No

Automatic           ⚡⚡⚡⚡      99%        ✅        ✅
Fallback            Fast + Smart               Yes
```

---

## Status Indicators in UI

```
┌─────────────────────────────────────────┐
│          PREDICTION DISPLAY             │
├─────────────────────────────────────────┤
│                                         │
│  Prediction: A                          │
│  Confidence: 95.23%                     │
│  Model Source: Backend API              │ ← Shows which engine
│                                         │
│  Top 3 Predictions:                    │
│  1. A     95.23%  ████████████████     │
│  2. B     02.15%  █                    │
│  3. C     00.92%  ▌                    │
│                                         │
│  Status: ✅ Backend API                │ ← Connection status
│                                         │
└─────────────────────────────────────────┘
```

---

## Error Handling Flow

```
Request to API
     │
     ↓
Network Available? ────NO──→ Use Client-side
     │                       TensorFlow.js
     YES                          │
     │                           ↓
     ↓                      Get prediction
API Responds?────NO──→ Use Client-side
     │                  TensorFlow.js
     YES                      │
     │                       ↓
     ↓                  Get prediction
Status 200? ────NO──→ Show Error Message
     │                  & Use Client-side
     YES                      │
     │                       ↓
     ↓                  Show result
Parse JSON                    
     │                       
     ↓                       
"success": true? ────NO──→ Show Error Message
     │                  & Use Client-side
     YES                     
     │                      
     ↓                      
Display                    
Prediction ✅              
```

---

## Deployment Architecture

```
For Production:

┌──────────────────────────────────────┐
│    Load Balancer / Reverse Proxy     │
│    (Nginx / Apache)                  │
└───────────────┬──────────────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
    ↓           ↓           ↓
┌────────┐ ┌────────┐ ┌────────┐
│ Next.js│ │ Next.js│ │ Next.js│
│ (Port │ │ (Port │ │ (Port │
│ 3000) │ │ 3001) │ │ 3002) │
└────────┘ └────────┘ └────────┘
    │           │           │
    └───────────┼───────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
    ↓           ↓           ↓
┌────────┐ ┌────────┐ ┌────────┐
│ Flask  │ │ Flask  │ │ Flask  │
│ (5000) │ │ (5001) │ │ (5002) │
└────────┘ └────────┘ └────────┘
    │           │           │
    └───────────┼───────────┘
                │
                ↓
    ┌──────────────────────┐
    │  Shared Model Storage│
    │  (Models loaded once)│
    │  (Shared via NFS/S3) │
    └──────────────────────┘
```

---

## Summary

This visual guide shows:
1. **How data flows** through the system
2. **How files are organized**
3. **How the setup process works**
4. **How to choose models**
5. **Technology stack** used
6. **Error handling** mechanisms
7. **Performance** characteristics
8. **Production** architecture

For detailed information, see the other documentation files!

---

**Ready to start?** Run: `START_MODEL_API.bat` 🚀
